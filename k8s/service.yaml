apiVersion: v1
kind: Service
metadata:
  name: oidc-validator
  namespace: oidc-validator
  labels:
    app: oidc-validator
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: oidc-validator
